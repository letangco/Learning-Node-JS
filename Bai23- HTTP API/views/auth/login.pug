extends ../layouts/common.pug
block content
        div
            h1 Login
            if errors
                each error in errors
                    .alert.alert-danger(role='alert')= error
            form(action="/auth/login" , method="POST")
                .form-group
                    label(for='exampleInputEmail1') Email
                    input#exampleInputEmail1.form-control(type='email',name="email", aria-describedby='emailHelp', value=values? values.email : "")
                .form-group
                    label(for='exampleInputPassword1') Password
                    input#exampleInputPassword1.form-control(type='password', name="password",value=values ? values.password: "")
                button.btn.btn-primary(type='submit') Login